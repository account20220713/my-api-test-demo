test:
  # ===== API 配置 =====
  base_url: https://jsonplaceholder.typicode.com
  timeout: 10

  # ===== UI 配置 =====
  ui_base_url: https://www.saucedemo.com/
  ui_timeout: 30
  headless: false
  browser: chromium
  slow_mo: 5000                      # ⭐ 从 1500 改为 5000（5秒）
  auto_detect_screen: true
  # launch_args: []                  # 手动配置时取消注释
  viewport:
    width: 1920
    height: 1080